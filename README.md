# NOTES

- Use `@` to supress display in terminal
- Use `tabs` for indentation

## Usage

- Availables commands : 
- - `make install`
- - `make git`
- - `make npm`
- - `make composer`
- - ` make uninstall`